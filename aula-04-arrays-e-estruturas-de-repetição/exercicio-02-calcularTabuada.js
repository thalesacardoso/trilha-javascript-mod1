// Exercícios de tabuada

// Declaração de variáveis
let numero = 4;
let total = 0;

// Laços de repetição com o for
for (let i = 1; i <= 10; i++) {
    total = numero * i;
    console.log(`A multiplicação de ${numero} x ${i} = ${total}`)
};